Refactor of an existing PHP + MySQL CMS website (not a rebuild).
Goal: Add a new public page and its full CMS controls, without breaking anything that already works.

Non-negotiables

DO NOT break or change: login/auth, existing pages, contact form logic, media upload, language toggle, current DB tables (only add minimal, additive fields/tables if strictly necessary).

Keep one global contact form component and render it at the bottom of every public page (above the footer).

Use the existing Media Picker (or URL) for all images in the new UI.

No hash anchors in nav; real routes only.

Bilingual stays as in the current project (EN/AR). Use the same translation approach already used.

New public page

Route (EN): /training-program — title: Training Program

Route (AR): /البرنامج-التدريبي — title: البرنامج التدريبي

Nav position: After Home then About Us. Add to footer Quick Links, too.

At the bottom: render the shared contact form block (the same one used elsewhere).

Visual guidance

Use the attached screenshots as layout inspiration for the sections (cards, headers, spacing, icons).

Use placeholder images that are editable via Media Picker/URL.

Do not hardcode copy; all text/images come from CMS fields.

Page sections (build these for the new page only)

Each section must have its own CMS editor with only the fields it actually uses (no dead CTA fields). All sections support: publish on/off and numeric sort order. All text is bilingual (EN/AR).

Hero – Training Program (with CTA)

Fields: title_en/ar, subtitle_en/ar, image (Media Picker/URL), CTA label_en/ar + CTA behavior: smooth scroll to the contact form on the same page.

Frontend: large headline, subhead, optional illustration on the side, CTA button that scrolls to the bottom contact form.

What you’ll learn / ماذا ستتعلم

Grid of 4–8 items.

Item fields: title_en/ar, body_en/ar (short text), icon_or_image (Media Picker/URL), sort_order, published.

Section editor provides CRUD for items (add, edit, delete, re-order).

Value / Bonuses (قيمة مضافة/مزايا)

4–6 cards explaining added value.

Card fields: title_en/ar, subtitle_en/ar (optional), body_en/ar, image (optional), sort_order, published.

Full CRUD for cards.

Student Outcomes / لماذا هذا البرنامج

3–6 highlights (cards or bullet tiles).

Item fields: title_en/ar, body_en/ar (short), optional icon_or_image, sort_order, published.

Full CRUD for items.

Testimonials — specific to this page (not the Home set)

Build a page-scoped testimonials manager to avoid coupling with Home.

Fields per testimonial: name (optional), role/company (optional), quote_en/ar (required), photo (optional), sort_order, published.

FAQ — specific to this page

Build a fresh FAQ block dedicated to this page.

Item fields: question_en/ar, answer_en/ar, sort_order, published.

Accordion UI on frontend; full CRUD in CMS.

Do not create a “Final CTA” section. We explicitly don’t need it.

Admin / CMS

In Admin → Page Sections, add a new page filter: Training Program  that lists only the sections of this page.

Each section gets its own editor with only the fields required by its frontend design.

Example: if a section has no button in UI, do not add CTA fields in the CMS.

For multi-item sections (Learn, Bonuses, Outcomes, Testimonials, FAQ), provide a child manager (list + Add/Edit/Delete + sort + publish toggles).

All images selectable by Media Picker or URL (both should work).

Sorting uses integer order (lowest shows first). No drag-and-drop needed now.

Frontend behavior

Hero CTA button performs smooth scroll to the bottom contact form block on this page.

All section content is fully fed from CMS.

Keep existing global styles; match spacing/typography with screenshots for a modern, clean look.

DB / Data

Prefer reusing current tables if they already match; otherwise add minimal additive tables:

page_sections entry for this page.

For section items (learn/bonuses/outcomes/testimonials/faq), add simple page-scoped tables if needed, with bilingual columns and sort/publish flags.

No destructive changes to existing schema or data.

Acceptance checklist (must all pass)

New page visible in nav + footer (EN/AR), routes work.

Sections render in this order: Hero → What you’ll learn → Value/Bonuses → Student Outcomes → Testimonials → FAQ → Contact form → Footer.

All text/images editable in CMS; no unused CTA fields appear where the frontend has no button.

Multi-item sections support add/edit/delete and sorting; publish toggles work.

Hero CTA scrolls to the contact form; no broken links.

No regressions in login, media upload, contact form, other pages.

Deliverables:

Updated PHP templates for the new page (EN/AR routes).

Admin pages/forms for the new sections and their items.

Minimal DB migration (additive only).

Notes where code was added and how to seed example content.

Use the reference images I’ll provide as design inspiration — create sections that are visually similar and well-aligned with their style, while keeping layouts consistent with the site’s overall design.
