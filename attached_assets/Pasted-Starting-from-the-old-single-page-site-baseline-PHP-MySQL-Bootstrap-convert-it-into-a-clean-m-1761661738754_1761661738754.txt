Starting from the old single-page site baseline (PHP + MySQL + Bootstrap), convert it into a clean multi-page Learn Data Analysis site with real pages (not sections), while keeping the current stack and the existing admin panel style. Extend the dashboard minimally to manage the new pages/sections/content. Do not introduce /en or /ar folders; keep the current bilingual mechanism (cookie / ?lang=) as is.

🔒 Hard rules

Do NOT change the tech (PHP, MySQL, Bootstrap 5, existing folder structure).

Do NOT rebuild the admin from scratch. Extend the current one minimally.

No /en or /ar path prefixes. Language stays via cookie / ?lang=.

No standalone /contact page. Any CTA for signup/registration must scroll/jump to the contact form at the top of the current page (#contact).

Fix admin include paths to use absolute paths with __DIR__ (eliminate require_once path errors).

Remove/disable old inline sections on Home for About and Contact (we are replacing them with a real about.php and a global footer contact block).

🗺️ Real pages to create (root level)

Create actual PHP files (not anchors inside Home):

index.php (Home)

about.php (About Us)

excel.php (Excel topic page)

power-bi.php (Power BI topic page)

statistics.php (Statistics topic page)

sql.php (SQL topic page)

content.php (explainer/article detail; route via ?topic=excel&slug=...)

All of these pages must include the same contact form component at the very top (same backend/store logic already used today).
Any CTA for “Enroll/Join/Apply/Register” must link to #contact (on the same current page).

🎨 Page content requirements (DB-driven; placeholders allowed)
1) Home (index.php)

Hero with headline + one CTA → #contact (no “Learn More” button).

Why Data Analysis: short persuasive copy (EN/AR), icons allowed.

Benefits: 3–4 cards (icon + short text).

Explore the Topics: link cards to Excel / Power BI / Statistics / SQL pages.

Testimonials: 2–3 quotes (reuse existing testimonials if present).

FAQ: 3–4 items (optional, reuse existing if present).

Contact form component at the top (same backend logic as before).

No embedded About or Contact sections inside Home—they are separate now.

2) About Us (about.php)

Independent page, not a copy or fallback of Home.

3–4 sections (intro, mission/vision, approach, image section).

Each section: title + text (EN/AR) + placeholder image.

Contact form component at the top.

3) Topic pages (excel.php, power-bi.php, statistics.php, sql.php)

Small hero (title + short intro + hero image).

At least 2 sections per topic page (intro section + practical uses/value).

A grid of explainers (cards) at the bottom (see “Seeding”).

Contact form component at the top.

4) Explainer detail (content.php?topic=...&slug=...)

Title, hero image, long HTML body (placeholder ok),

“Related items” (same topic, simple nearest order).

Contact form component at the top.

Show 404 friendly page when topic/slug not found.

5) Footer (site-wide)

Move the entire Contact block (from old Home) into the footer:

Map, social links, email, phone — move all of it intact.

Also include:

Site Links: Home, About, Excel, Power BI, Statistics, SQL.

Short extra blurb (third column), e.g., a one-line about/mission statement.

All footer texts/links must be editable from the admin (see Admin section).

🧰 Admin panel (extend minimally; keep current structure)

Goal: give full control over the new pages and content, without rebuilding.

Settings

Editable contact_recipient_email (used by the existing contact form backend).

Logo (select from existing Media upload).

Social links.

Home Content and About Content

Manage sections (enable/disable).

Numeric display_order (no drag & drop now).

EN/AR titles/subtitles/body.

Image picker (reuse existing Media Upload with a simple “choose from media”).

Topics

For each tool page (Excel, Power BI, Statistics, SQL):

EN/AR title, short intro, hero image.

Content Items (Explainers)

Per topic: CRUD with fields:

EN/AR title, summary, body (textarea ok), hero image, status (draft/published), numeric order, slug.

Cards on topic pages must read from these items.

Footer

Edit short about text (EN/AR).

Edit contact info (email/phone/address), social links, and show/hide map block.

Site links are the fixed 6 pages above (just ensure they render).

Media

Reuse the existing media upload page; all pickers use a simple “choose from media” UI.

Admin include paths

Replace all relative require_once with absolute paths using __DIR__ to stop include errors:

// Example in admin/includes/header.php
require_once __DIR__ . '/auth.php';
require_once __DIR__ . '/db.php';
// Example from admin/layout/*.php to header.php
require_once __DIR__ . '/../includes/header.php';


Apply consistently across all admin files.

🌱 Seeding (minimal but complete)

About: seed 3–4 sections with placeholders (EN/AR).

Each topic page: seed 1–2 explainers (EN/AR) with placeholder image and short content (a few paragraphs max).

Ensure all seeded items are published so pages are not empty.

Content length policy: Keep seeds short; I will add real content later from the admin.

🧭 Routing & language (keep current, simple)

Do not create /en or /ar folders.

Keep the existing bilingual mechanism (cookie / ?lang=).

Language switcher must preserve the current page and toggle ?lang.

Make sure all public pages and admin work under this scheme.

✅ Acceptance criteria (must pass)

All 6 pages exist and are reachable from the navbar: index.php, about.php, excel.php, power-bi.php, statistics.php, sql.php.

About has its own content (not a Home fallback).

No /contact route anywhere. Any CTA for enrolment/registration points to #contact (the form at the top of the current page).

Topic pages show non-empty grids (seeded cards). Cards open a working content.php?topic=...&slug=... page.

Footer contains the full contact block (map + socials + email/phone) plus site links and a short about text—visible on all pages.

Admin opens with no include path errors. From admin I can:

Change logo, social links, contact_recipient_email.

Edit Home & About sections (enable/disable/order/texts/images).

Edit Topics (title/intro/hero image).

CRUD Content Items (fields above) and see changes reflected on topic/detail pages.

All texts and images shown on the frontend are DB-driven (not hardcoded), and placeholders are fine.

Keep the current contact form backend exactly as is (same storage/sending), just render the component at the top of every page.

📄 Deliverables

List of files changed/added and where to find the new admin screens.

SQL/migration snippets if any minor additions were needed (idempotent).

Short README note: how to edit Home, About, Topics, Items, Footer in admin.

Execute exactly as specified. Avoid previous mistakes: no About=Home fallback, no /contact, no /en//ar path prefixes, no broken admin includes.