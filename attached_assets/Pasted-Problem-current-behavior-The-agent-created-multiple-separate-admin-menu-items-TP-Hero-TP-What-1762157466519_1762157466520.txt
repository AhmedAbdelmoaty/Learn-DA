Problem (current behavior):
The agent created multiple separate admin menu items (TP: Hero, TP: What You’ll Learn, TP: Value & Bonuses, …). These links either don’t map to real admin pages or aren’t wired to the current auth flow, so clicking them often redirects to Login. Also, each section shows generic fields (e.g., CTA) even when the frontend section has no button.

Goal (what we want):
Make one single admin page for Training Program that contains tabs for all sections. Each tab has an editor that matches that section’s real structure (no unused fields). Keep everything else intact.

Non-negotiables

Admin-only change. Do NOT modify the public frontend (the Training Program page is already built).

Keep existing auth, media upload, contact form, and all other admin pages working.

Minimal, additive DB only (no destructive changes).

Build exactly this
1) One admin entry

In the admin sidebar, remove the multiple TP links.

Add one item: Training Program → admin/training_program.php.

2) Single admin page with tabs

Create admin/training_program.php and include standard admin boilerplate:

require_once __DIR__ . '/includes/auth.php';
require_once __DIR__ . '/includes/header.php';


Use Bootstrap tabs or a left sub-nav inside this single page.

Tabs (in order): Hero • What You’ll Learn • Value & Bonuses • Outcomes • Testimonials • FAQ.

3) Section editors (match each section’s structure)

All text is bilingual (EN/AR) where it appears to users. Every section supports is_published and a numeric sort_order where applicable. Only show fields that the frontend actually uses.

Hero (has CTA):
Fields: title_en/ar, subtitle_en/ar, image (Media Picker or URL), cta_label_en/ar, cta_link (default #contact), is_published.

What You’ll Learn (cards grid):
Parent fields (optional): title_en/ar, subtitle_en/ar, is_published.
Items CRUD table (modal add/edit): icon_class (Font Awesome) or image, title_en/ar, body_en/ar, sort_order, is_published.

Value & Bonuses (cards):
Items CRUD table: title_en/ar, subtitle_en/ar (optional), body_en/ar, optional image, sort_order, is_published.

Outcomes / Why this program (highlights):
Items CRUD table: title_en/ar, optional body_en/ar, optional icon_class or image, sort_order, is_published.

Testimonials (specific to Training Program):
Let the admin select which existing testimonials to show for this page (multi-select or a small linking table) and control sort_order, is_published.
Do not duplicate testimonial content; use a junction/links table.

FAQ (specific to Training Program):
Items CRUD table: question_en/ar, answer_en/ar, sort_order, is_published.
(Dedicated table for TP FAQ; do not reuse Home FAQ.)

4) Data model (additive only)

If not already present, add small section-specific tables, e.g.:

tp_learn_items(id, title_en, title_ar, body_en, body_ar, icon_class, image_url, sort_order, is_published, created_at, updated_at)

tp_value_items(...)

tp_outcome_items(...)

tp_testimonial_links(id, testimonial_id, sort_order, is_published)

tp_faq_items(id, question_en, question_ar, answer_en, answer_ar, sort_order, is_published, created_at, updated_at)

Use the existing Media Picker for image fields. When selecting from Media, save an absolute URL (so saving doesn’t fail validation that expects a URL).

5) UX details

Stay on the same tab after save; show success/error toast.

Numeric sort_order is enough (no drag-and-drop needed).

Respect RTL inputs for Arabic fields.

Remove the old sidebar entries (TP: Hero, TP: …).

6) Frontend (do not change)

No frontend template or CSS changes now.

The existing public Training Program page already renders; it should just read from these new TP tables.

Hide a section on the public page when its is_published = 0.

Acceptance checklist

One sidebar item Training Program opens admin/training_program.php (no login redirect).

Tabs appear inside that single page; each tab has an editor that matches the section’s real layout.

Cards/FAQ/Testimonials tabs provide proper CRUD, publish toggles, and sorting.

Image fields work with Media Picker or URL; absolute URLs are saved correctly.

No changes or regressions in other admin areas or public pages.