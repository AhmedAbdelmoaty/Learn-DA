Keep current stack and structure (PHP + MySQL + Bootstrap).
1) Move the Contact Form (all public pages)

The existing contact form component should appear at the bottom of every page, just above the footer (not at the top anymore).

Pages: Home (index.php), About (about.php), tool pages (excel.php, power-bi.php, statistics.php, sql.php), and content detail (content.php).

Add an anchor id on the form container: id="contact".

Any CTA that previously scrolled to the top form must now link to #contact (same-page anchor).

Do not change the form’s current backend logic for storage/sending.

2) Add a “Course Rounds” dropdown to the form

Insert one new field (dropdown) in the form:

Label (EN): Choose the course date

Label (AR): اختَر موعد الدورة

Field name (POST): selected_round_id (value = round ID)

Position: after Phone field and before Message field.

No placeholder text (other fields don’t use placeholders).

Behavior:

If there are published rounds → show the dropdown (you can set it required).

If there are no published rounds → hide the dropdown and show a small note:

EN: No upcoming rounds available at the moment.

AR: لا توجد مواعيد متاحة حاليًا.

On submit:

Save both:

selected_round_id (INT, nullable)

selected_round_label (VARCHAR(255), nullable) — the user-visible label at the time of submission, matching current language.

Include the selected round in the sent email (if emailing is enabled):

EN: Selected round: <label>

AR: ميعاد الدورة المختار: <label>

3) Minimal DB additions (idempotent)

Create a small table for rounds and add two optional columns to messages. Keep everything non-destructive and safe to re-run.

Table: course_rounds

id INT PK AI

label_en VARCHAR(255) NOT NULL

label_ar VARCHAR(255) NOT NULL

start_at DATETIME NULL

published TINYINT(1) NOT NULL DEFAULT 0

sort_order INT NOT NULL DEFAULT 0

created_at DATETIME NULL

updated_at DATETIME NULL

Alter existing contact/messages table (name it as used today, e.g. contact_messages):

Add selected_round_id INT NULL

Add selected_round_label VARCHAR(255) NULL

No other schema changes.

4) Admin panel — simple CRUD for rounds

Add a new admin menu item “Course Rounds” (place it where Topics/Content are).

List (order by published DESC, then start_at ASC, then sort_order ASC)

Add / Edit / Delete

Fields: label_en, label_ar, start_at (datetime), published (on/off), sort_order (numeric).

Bilingual labels are edited here. The frontend shows label_en or label_ar based on current language.

No advanced validation. No archive logic. Keep it simple.

All includes in admin must use absolute __DIR__ paths (avoid require_once errors).

5) Frontend dropdown data

On rendering the form, load published rounds only.

Show the label for the current language (label_en or label_ar).

Sort: published DESC, then by start_at ascending, then sort_order ascending (or just sort_order if start_at is null).

6) Language handling

Use the existing language mechanism (cookie / ?lang=).

The form’s dropdown label and the displayed round names must respect the current language.

No routing changes required.

7) Acceptance criteria

Contact form appears at the bottom of every public page, above the footer, with id="contact".

The new dropdown (label per language) is shown after Phone and before Message when there are published rounds; otherwise it’s hidden with a short note.

Submissions store both selected_round_id and a snapshot selected_round_label, and the email includes the selected round.

Admin has a Course Rounds screen with list/add/edit/delete and a publish toggle.

No PHP warnings/notices; admin includes use __DIR__.

No breaking changes to existing forms or DB beyond the specified table/columns.

8) Deliverables

List of changed/added files (frontend + admin + migrations).

SQL snippets (migrations) used to create/alter tables (idempotent).

Quick note: where to manage rounds in admin, and confirmation that the form shows/hides the dropdown as intended.